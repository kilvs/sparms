{%- if section.settings.show_section -%}
<section class="page-collection-stub padding-global padding-section-medium">
  <div class="container-large">
    <div class="text-align-center">
      <h1>{{ section.settings.heading | default: 'Collection' }}</h1>
      <div class="spacer-medium"></div>
      <p>{{ section.settings.body | default: 'Collection grid will appear here once products are added.' }}</p>
    </div>
  </div>
</section>
{%- endif -%}

{% schema %}
{
  "name": "Page — Collection",
  "tag": "section",
  "class": "shopify-section-page-collection",
  "settings": [
    { "type": "checkbox", "id": "show_section", "label": "Show this section", "default": true },
    { "type": "text", "id": "heading", "label": "Heading", "default": "Collection" },
    { "type": "textarea", "id": "body", "label": "Body", "default": "Collection grid will appear here once products are added." },
    { "type": "paragraph", "content": "Stub section — wire up commerce / dynamic content per CONVERSION_GUIDE §7." }
  ],
  "presets": [{ "name": "Page — Collection" }]
}
{% endschema %}
